# 날짜별 수정사항

**제품:** Jokguboard  
**정리일:** 2026-09-02  

상세 로그는 저장소 루트 `수정기록.txt`. APK 파일명 순서는 [jokgu-apk.md](jokgu-apk.md).

모든 항목은 2026-09-02 (KST)이다. 아래는 시간 순(이른 → 늦은)이다.

## 2026-09-02

### 오후 13:34 — MVP

그린필드 웹 점수판 + Android WebView. 족구 15점·14:14 듀스·3세트 2선승. 선서브, 되돌리기, 새 경기, ko/en/zh, 도움말 3종. Vercel `/play`.  
APK: `app-debug-0902_1334-jokgu-mvp.apk`

### 오후 13:57 — 보드 모습

청 어두운 카드·백 밝은 카드. 탁구 HTML 미복사.  
APK: `app-debug-0902_1357-board-look.apk`

### 오후 14:08 — 모습 4종

설정에서 기본·네온·카드·경기장.  
APK: `app-debug-0902_1408-look-pack.apk`

### 오후 14:30 — TTS·BLE·심플·서체

점수 읽기, BLE 시계 패킷, 심플 모드, OFL 4종, 세트제.  
APK: `app-debug-0902_1430-tts-ble.apk`

### 오후 14:49 — 카드 3D

사이버·링 원근. 경기장·방송 SVG 테두리. PNG 없음.  
APK: `app-debug-0902_1449-card-depth.apk`

### 오후 15:08 — 원·회전

원 1~4, 사이드아웃 로테이션 연결. (이후 4번도 코트 선수, 숫자는 고정으로 바뀜.)  
APK: `app-debug-0902_1508-rotate.apk`

### 오후 15:19 — 스킨 CSS

자체 Score 스킨 색·테두리를 족구 DOM에 연결. 카메라·PNG 없음.  
APK: `app-debug-0902_1519-skins.apk`

### 오후 15:50 — 하단 메뉴

메뉴를 보드 아래. 언어는 설정. −1 버튼 삭제(길게 −1). 점수 확대.  
APK: `app-debug-0902_1550-dock.apk`

### 오후 16:00 — 가운데 새 경기

새 경기를 세트 칸으로. 심플에서 새 경기 숨김.  
APK: `app-debug-0902_1600-mid.apk`

### 오후 16:18 — 종목 전환

한 보드에서 족구 / Futnet.  
APK: `app-debug-0902_1618-sport.apk`

### 오후 16:31 — 새 경기 배너

브라우저 `confirm` 대신 보드 취소·확인.  
APK: `app-debug-0902_1631-fresh.apk`

### 오후 16:36 — 새 경기 클릭

0:0 선서브 대기에서 버튼이 눌리게. 금색 큰 버튼.  
APK: `app-debug-0902_1636-freshhit.apk`

### 오후 16:46 — 가운데 z-index

좌우 큰 점수가 새 경기를 덮지 않게.  
APK: `app-debug-0902_1646-midz.apk`

### 오후 17:12 — 사이버 클릭

사이버·링에서 가운데 3D 뒤로 밀림을 해제해 새 경기 클릭.  
APK: `app-debug-0902_1712-cyberhit.apk`

### 오후 17:38 — 네트쪽 원·서브 규칙

원 숫자 고정, 사이드아웃 시 직전 서브 팀 1→2→3→4. (원 위치는 곧 바깥으로 되돌림.)  
APK: `app-debug-0902_1738-netcol.apk`

### 오후 17:46 — 원 바깥·크게

원을 바깥으로. 원·숫자 확대. 서브 원·숫자 더 큼·금색.  
APK: `app-debug-0902_1746-outdisc.apk`

## 2026-09-08

### 오후 20:10 — HID 패드

APK 설정에 위·가운데·아래 버튼 위치 맞추기. 외부 BT 음량 짧게 좌우 득점, 길게 되돌리기. 원형 미디어 짧게 되돌리기. 웹에는 설정 칸 없음.  
APK: `app-debug-0908_2010-hidpad.apk`

## 2026-09-09

### 오후 17:01 — 코트 원 2×2

선수 원을 점수 숫자 위·아래에 둔다. 화면 왼쪽 위 3·4 / 아래 2·1, 오른쪽 위 1·2 / 아래 4·3. 선서브 금색 원은 1번부터.  
APK: `app-debug-0909_1701-courtdisc.apk`

### 오후 22:34 — 위·가운데·아래 패드 제스처 인식

갤럭시 탭 S9+ 실기기 테스트로 리모컨의 실제 동작 방식을 확인: 위 버튼은 좌표 없는 단독 시스템 터치-아웃사이드 신호로만 오고, 가운데·아래는 눌림→뗌 사이 경과시간·이동거리로 구분되는 제스처(탭/스와이프)다. 좌표 비교 방식(2점·3점 모두)을 걷어내고 이 방식으로 다시 구현. 보정은 가운데·아래 2단계만 남음. 실기기에서 위/가운데/아래 정상 동작 확인.  
APK: `app-debug-0909_2234-padgesture.apk`

### 오후 22:56 — 세트 자동 닫힘·경기 승 새 경기

세트 종료 안내는 확인 없이 5초 뒤 닫혀 다음 세트를 칠 수 있다. 경기 전체가 끝나면 확인(결과 유지)과 새 경기(바로 초기화)를 고른다. 패드 제스처 인식은 그대로.  
APK: `app-debug-0909_2256-winflow.apk`

### 오후 23:31 — 패드 고정·세트 안내 중 득점 (현재)

`MainActivity.kt`는 `2234-padgesture` 그대로. 세트 안내 창이 떠 있어도 위·가운데·아래 득점이 막히지 않게 HTML만 수정.  
APK: `app-debug-0909_2331-padwin.apk`

### 오후 23:57 — 팀명·새 경기 선서브

팀명을 가운데 세트 쪽으로 옮기고 글자를 2배로 키움(사이버 스킨은 위치 유지, 글자만 2배). 팀명 터치는 이름 수정. 선서브는 원·화면 좌우·패드 위·아래. 새 경기 카드에서 선서브 팀을 고르면 바로 시작.  
APK: `app-debug-0909_2357-nameserve.apk`

### 오전 00:29 — 원 간격·점수 칸 맞춤

원 번호 배치는 유지하고 위·아래 원을 같은 세로줄로 점수 가운데에 모음. 큰 점수는 남은 칸 높이에 맞춤.  
APK: `app-debug-0910_0029-discscore.apk`

### 오전 06:28 — 점수 굵기·+1 확대

기본 모습 점수를 더 굵게 하고 층 그림자를 넣음. +1이면 숫자가 잠깐 커지고 +1 표식이 올라감. 배드민턴 HTML은 복사하지 않음.  
APK: `app-debug-0910_0628-scorebump.apk`

### 오전 10:49 — 사이버 터치·그림자 약 30%

사이버·링에서 가운데 칸이 팀명·점수를 가로채지 않게 함. 점수 층 그림자는 이전의 약 30%.  
APK: `app-debug-0910_1049-hitdepth.apk`

### 오전 11:18 — 세트 칸 비율 글자·시각·경기시간

가운데 「세트」·서브 안내·시각·경기 시간은 칸 크기에 맞추고, 세트 점수 `0 : 0`은 남은 칸을 채움. 경기 시간은 선서브부터, 새 경기에서 0. 새 경기 글자는 버튼에 맞춤.  
APK: `app-debug-0910_1118-midfit.apk`

### 오전 11:45 — 세트 점수 키움·세로 가운데 모임

가운데 칸 폭은 그대로. 시각·경기 시간·서브 안내·새 경기를 세트 점수 바로 아래로 모아 세로 가운데에 둠. 세트 점수는 칸 안에서 직전보다 크게(요청 1.7배는 좌우 점수를 가려 칸에 맞춤).  
APK: `app-debug-0910_1145-setmid.apk`

### 오후 13:03 — 세트 안쪽 모임·심플 서브 원·화살표

세트 점수 숫자는 크기 그대로 콜론 쪽으로 모음. 심플에서는 코트 원을 숨기고 가운데에 금색 서브 번호와 서브 팀 쪽 CSS 삼각형 화살표.  
APK: `app-debug-0910_1303-servemark.apk`

### 오후 13:26 — 서브 바·원 확대·가운데 문구 제거

가운데 칸의 「청 서브」류 문구와 화살표를 없앰. 심플 가운데 서브 원은 1.5배. 일반·심플 모두 서브 팀 큰 점수 아래에 짧은 막대(어두운 칸 흰색, 밝은 칸 검은색).  
APK: `app-debug-0910_1326-servebar.apk`

### 오후 13:46 — 서브 바 숫자 붙임·세트·시계 글자

서브 막을 점수 숫자에 붙이고 두께·길이를 1.5배. 「세트」 3배. 시각·경기 시계 세로는 2배·가로는 1.3배. 어두운 점수 칸 흰 바, 밝은 칸 검은 바.  
APK: `app-debug-0910_1346-midtype.apk`

### 오후 13:56 — 서브 바 흰 칸만 검정

스킨에서 백 칸이 어두워도 흰 바가 나오게. 검은 바는 기본 모습의 흰 점수 칸만. `look-*`가 아니라 `skin-*`로 붙는 모습을 맞춤.  
APK: `app-debug-0910_1356-barwhite.apk`

### 오후 14:05 — 심플 서브 원 맨 위·숫자 1.5배

심플 모드 금색 서브 원을 가운데 칸 맨 위(「세트」 위)로 옮김. 원 안 숫자는 1.5배. 멀리서 원이 먼저 보이게.  
APK: `app-debug-0910_1405-servetop.apk`

### 오후 15:12 — 밝은 백 칸 검은 서브 바

링(`skin-arena3d`)·경기장(`look-field`)처럼 백 칸이 흰·밝은데 바가 흰색이던 것을 검은 바로. 기본 백 칸과 같음.  
APK: `app-debug-0910_1512-barlight.apk`

## 2026-09-12

### 오후 16:20 — 위 버튼 뒤에만 시스템 바 닫기

3버튼 리모컨에서 위 버튼으로 판정된 뒤에만 시스템 바를 바로 닫음. 손가락으로 연 바는 그대로.  
APK: `app-debug-0912_1620-baruphide.apk`

### 오후 16:35 — 위 버튼 바 빠른 닫힘·패드 커서 숨김

위 버튼 판정 뒤 시스템 바를 바로 닫고, OS가 늦게 켜면 16ms 간격으로 최대 200ms 동안 다시 닫음. 맞춤이 켜진 동안 3버튼 리모컨 마우스 커서는 숨김. 손가락으로 연 바는 그대로.  
APK: `app-debug-0912_1635-barcursor.apk`

### 오후 17:01 — 위 버튼 바 0.8초 창·켜진 즉시 닫기 (현재)

200ms 반복 숨김은 OS 열림 애니보다 먼저 끝나 체감이 그대로였음. 위 판정 뒤 0.8초 동안만 바가 화면에 나타난 순간에 닫고, 같은 창 안에서 16ms 재시도. 손가락으로 연 바는 창 밖이라 그대로. 커서 숨김은 유지.  
APK: `app-debug-0912_1701-barwatch.apk` ← **현재 설치**

### 문서만 (소스 없음) — 2026-09-02

PRD·plan·날짜별·APK별 문서를 당시 제품 기준으로 맞춤. 그 커밋은 앱·APK 소스 변경 없음.
