# 수정기록 — Jokguboard

## 2026-09-10 서브 바 숫자 붙임·세트·시계 글자 (JK_SERVE_BAR / JK_MID_FIT)

족구 보드 자체 HTML/CSS만 변경. `MainActivity.kt` 미수정. `C:\Score` · `C:\PickleBoard` · `C:\sports_scoreboard` 미수정. 외부 구현 미복사.

서브 막대를 `.score-glyph::after`로 옮겨 점수 숫자에 붙임. 두께 15px(심플 18px)·폭 `48cqw`(이전 1.5배). 어두운 점수 칸 흰색, 밝은 칸 검은색. 「세트」 라벨 3배. `.mid-clocks` 글자 가로 1.3배·`scaleY(2/1.3)`으로 세로 2배. 세트 `0:0` 크기는 유지.

### APK
`app-debug-0910_1346-midtype.apk`

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1(Noto Sans KR, Jua, Do Hyeon, Orbitron). AndroidX Apache-2.0. Play 스토어 제출용이 아닌 debug APK.

### 보안
카메라·마이크 없음. 팀명 `textContent`. 사용자 HTML 삽입 없음.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, help 3종, `index.html`, docs, Android assets www
- 신규 파일: `downloads/app-debug-0910_1346-midtype.apk`
- 재사용: 기존 `JK_SERVE_BAR` · `JK_MID_FIT` · `is-serving`
- 미수정: `MainActivity.kt`
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
`score-glyph::after` + `JK_SERVE_BAR` + `scaleY(1.5384615)` 묶음은 이 저장소 전용 조합.

### 특허 (검토 여지 · 비침해 보장 아님)
- 외부 리모컨의 눌림→뗌 경과시간·이동거리로 탭/스와이프 구분
- 좌표 없는 단독 시스템 터치-아웃사이드 신호를 특정 버튼 눌림으로 해석
- 점수 아래 짧은 막대·비균등 글자 확대는 일반 UI 관용에 가깝지만 별도 표시

---

## 2026-09-10 서브 바·원 확대·가운데 문구 제거 (JK_SIMPLE / JK_SERVE_BAR)

족구 보드 자체 HTML/CSS/JS만 변경. `MainActivity.kt` 미수정. `C:\Score` · `C:\PickleBoard` · `C:\sports_scoreboard` 미수정. 외부 구현 미복사. 외부 아이콘·SVG 미사용.

가운데 `#serveGuide`의 `{팀명} 서브` 문구를 비움(선서브 안내는 유지). 심플 화살표 HTML/CSS 삭제. 가운데 금색 서브 원을 1.5배. 일반·심플 모두 `.side.is-serving .point-num::after`로 점수 아래 짧은 막대. 기본 청 칸 흰색·백 칸 검은색. 종이 모습은 양쪽 검은색. 네온·아레나·방송·사이버의 어두운 백 칸은 흰색. 선서브 전에는 막대 없음. `pointer-events: none`.

### APK
`app-debug-0910_1326-servebar.apk` (`29bcc40`)

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1(Noto Sans KR, Jua, Do Hyeon, Orbitron). AndroidX Apache-2.0. Play 스토어 제출용이 아닌 debug APK.

### 보안
카메라·마이크 없음. 팀명 `textContent`. 서브 번호는 `serverSlotFor`의 숫자만 `textContent`. 사용자 HTML 삽입 없음.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, help 3종, `index.html`, docs, Android assets www
- 신규 파일: `downloads/app-debug-0910_1326-servebar.apk`
- 재사용: 기존 `JK_SIMPLE` · `is-serving` · `paintJkServeMark` · `.jk-serve-num`
- 미수정: `MainActivity.kt`
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
`jk-serve-num` + `paintJkServeMark` + `.point-num::after` 서브 막대는 이 저장소 전용 조합.

### 특허 (검토 여지 · 비침해 보장 아님)
- 외부 리모컨의 눌림→뗌 경과시간·이동거리로 탭/스와이프 구분
- 좌표 없는 단독 시스템 터치-아웃사이드 신호를 특정 버튼 눌림으로 해석
- 서브 팀 점수 아래 짧은 막대는 일반 전광판 관용에 가깝지만 별도 표시

---

## 2026-09-10 세트 안쪽 모임·심플 서브 원·화살표 (JK_MID_FIT / JK_SIMPLE)

족구 보드 자체 HTML/CSS/JS만 변경. `MainActivity.kt` 미수정. `C:\Score` · `C:\PickleBoard` · `C:\sports_scoreboard` 미수정. 외부 구현 미복사. 외부 아이콘·SVG 미사용.

세트 점수 `font-size`는 유지하고 `.set-fit`을 `inline-flex` + 좁은 `gap`으로 콜론 쪽에 모아 칸 테두리에서 떼어 냄. 심플 모드에서 코트 `.rot-dot`은 계속 숨김. 가운데 칸에 금색 원(`#jkServeNum`)으로 서브 슬롯(1~4)을 표시하고, CSS `border` 삼각형으로 화면상 서브 팀 쪽을 가리킴(`teamsSwapped` 반영). 선서브 전에는 표시 없음. 일반 모드에서는 이 표시를 숨김.

### APK
`app-debug-0910_1303-servemark.apk` (`01689e1`)

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1(Noto Sans KR, Jua, Do Hyeon, Orbitron). AndroidX Apache-2.0. Play 스토어 제출용이 아닌 debug APK.

### 보안
카메라·마이크 없음. 팀명 `textContent`. 서브 번호는 `serverSlotFor`의 숫자만 `textContent`. 사용자 HTML 삽입 없음.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, help 3종, `index.html`, docs, Android assets www
- 신규 파일: `downloads/app-debug-0910_1303-servemark.apk`
- 재사용: 기존 `JK_SIMPLE` · `JK_MID_FIT` · `serverSlotFor` · `computeServeTeam` · `.rot-dot.is-server` 금색
- 미수정: `MainActivity.kt`
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
`jk-serve-mark` + `paintJkServeMark` 묶음은 이 저장소 전용 명칭. CSS `border` 삼각형과 서브 쪽 화살표는 일반 UI 관용.

### 특허 (검토 여지 · 비침해 보장 아님)
- 외부 리모컨의 눌림→뗌 경과시간·이동거리로 탭/스와이프 구분
- 좌표 없는 단독 시스템 터치-아웃사이드 신호를 특정 버튼 눌림으로 해석
- 서브 팀을 화살표로 가리키는 구성은 일반 점수판 관용에 가깝지만 별도 표시

---
## 2026-09-10 세트 점수 1.7배·세로 가운데 모임 (JK_MID_FIT)

족구 보드 자체 HTML/CSS만 변경. `MainActivity.kt` 미수정. `C:\Score` · `C:\PickleBoard` · `C:\sports_scoreboard` 미수정. 외부 구현 미복사.

가운데 칸 폭(`clamp` 최대 292px)은 그대로. 세트 줄의 `1fr` 남은 칸을 없애고 `align-content: center`로 「세트」·점수·시각·경기·서브·새 경기를 한 덩어리로 세로 가운데에 모음. 세트 점수는 직전 `46cqw`에서 칸 안에 들어가는 크기(`50cqw`)로 키움. 요청한 1.7배(`78cqw`)는 `0 : 0`이 좌우 큰 점수를 덮어 적용하지 않음. 칸 쿼리를 못 쓰는 환경의 `clamp` 폴백은 1.7배.

### APK
`app-debug-0910_1145-setmid.apk` (`caa51ae`)

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1(Noto Sans KR, Jua, Do Hyeon, Orbitron). AndroidX Apache-2.0. Play 스토어 제출용이 아닌 debug APK.

### 보안
카메라·마이크 없음. 팀명 `textContent`. 시계 숫자는 `textContent`. 사용자 HTML 삽입 없음.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, help 3종, `index.html`, docs, Android assets www
- 신규 파일: `downloads/app-debug-0910_1145-setmid.apk`
- 재사용: 기존 `JK_MID_FIT` · `.set-fit` · `.mid-clocks` · `#btnFresh`
- 미수정: `MainActivity.kt`
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
`align-content: center` + `78cqw` 세트 점수 묶음은 일반 CSS. 외부 보드 구현 미복사.

### 특허 (검토 여지 · 비침해 보장 아님)
- 외부 리모컨의 눌림→뗌 경과시간·이동거리로 탭/스와이프 구분
- 좌표 없는 단독 시스템 터치-아웃사이드 신호를 특정 버튼 눌림으로 해석

---
## 2026-09-10 세트 칸 비율 글자·시각·경기시간 (JK_MID_FIT)

족구 보드 자체 HTML/CSS/JS만 변경. `MainActivity.kt` 미수정. `C:\Score` · `C:\PickleBoard` · `C:\sports_scoreboard` 미수정. 외부 구현 미복사.

가운데 세트 칸을 세로 격자(`container-type: size`)로 두고, 「세트」·서브 안내·듀스·시각·경기 시간은 칸 너비 비율(`cqw`)로 맞춤. 세트 점수 `0 : 0`은 남은 칸(`minmax(0,1fr)` + `min(cqw,cqh)`)을 채움. 「새 경기」는 버튼 자체를 컨테이너로 두고 글자를 버튼 크기에 맞춤(버튼 높이는 `vmin`이라 순환 참조 없음). 현재 시각은 기기 시계. 경기 시간은 선서브(`setServeChoice`)에서 시작, 새 경기·종목 리셋에서 0, `localStorage` 스냅샷에 `matchClockAt` 저장. 칸 쿼리를 못 쓰는 환경은 `clamp` 폴백.

### APK
`app-debug-0910_1118-midfit.apk` (`57f140e`)

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1(Noto Sans KR, Jua, Do Hyeon, Orbitron). AndroidX Apache-2.0. Play 스토어 제출용이 아닌 debug APK.

### 보안
카메라·마이크 없음. 팀명 `textContent`. 시계 숫자는 `textContent`. 사용자 HTML 삽입 없음. 시각/경기 라벨만 `data-i18n`(시간 값은 라벨 밖에 두어 `applyLang`이 지우지 않음).

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, help 3종, `index.html`, docs, Android assets www
- 신규 파일: `downloads/app-debug-0910_1118-midfit.apk`
- 재사용: 기존 `.mid` · `#btnFresh` · `setServeChoice` · `runFreshMatch` · `snapshot`/`restore` · `JK_SCORE_FILL` 칸 쿼리 패턴
- 미수정: `MainActivity.kt`
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
`JK_MID_FIT` + `matchClockAt` + `paintMidClocks` 묶음은 공개 검색에서 미발견(검색일 2026-09-10). `container-type`/`cqw`/`cqh`와 `Date` 시계 표시는 일반 CSS/JS.

### 특허 (검토 여지 · 비침해 보장 아님)
- 외부 리모컨의 눌림→뗌 경과시간·이동거리로 탭/스와이프 구분
- 좌표 없는 단독 시스템 터치-아웃사이드 신호를 특정 버튼 눌림으로 해석
- 선서브 시각을 경기 경과 원점으로 두는 구성은 일반 스톱워치 관용에 가깝지만 별도 표시

---
## 2026-09-10 사이버 터치·그림자 약 30% (JK_CYBER_HIT / JK_SCORE_DEPTH)

족구 보드 자체 HTML/CSS만 변경. `MainActivity.kt` 미수정. `C:\Score` · `C:\PickleBoard` · `C:\sports_scoreboard` 미수정. 외부 구현 미복사.

사이버 CSS가 Score의 `.team-header`를 가리켜 팀명 클릭 복구가 적용되지 않던 점을, 족구 DOM(`.team-title` / `.point-num`)에 맞춘 히트 규칙으로 보완. 가운데 세트 칸은 빈 곳은 통과하고 버튼만 받음. 점수 층 오프셋·획은 직전 값의 약 30%.

### APK
`app-debug-0910_1049-hitdepth.apk` (`5518ad6`)

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1. AndroidX Apache-2.0. Play 스토어 제출용이 아닌 debug APK.

### 보안
카메라·마이크 없음. 팀명 `textContent`. 사용자 HTML 삽입 없음.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, help 3종, `index.html`, docs, Android assets www
- 신규 파일: `downloads/app-debug-0910_1049-hitdepth.apk`
- 재사용: 기존 `.team-title` · `.point-num` · `.mid` · `JK_SCORE_DEPTH`
- 미수정: `MainActivity.kt`
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
`JK_CYBER_HIT` + `team-title` + `pointer-events: none` 가운데 칸 묶음은 공개 GitHub에서 미발견(검색일 2026-09-10). `pointer-events` 통과는 일반 CSS.

### 특허 (검토 여지 · 비침해 보장 아님)
- 외부 리모컨의 눌림→뗌 경과시간·이동거리로 탭/스와이프 구분
- 좌표 없는 단독 시스템 터치-아웃사이드 신호를 특정 버튼 눌림으로 해석

---
## 2026-09-10 점수 굵기·층 그림자·+1 확대 (JK_SCORE_DEPTH / JK_SCORE_BUMP)

족구 보드 자체 HTML/CSS/JS만 변경. `MainActivity.kt` 미수정. `C:\Score` · `C:\PickleBoard` · `C:\sports_scoreboard` 미수정. 외부 구현 미복사(배드민턴 클래스명·키프레임·주석 미사용).

기본 모습 점수에 획(stroke)과 층 그림자를 넣음. Jua는 단일 굵기라 `font-weight: 800`만으로는 두꺼워지지 않아 획+그림자로 처리. Noto Sans KR ExtraBold(800)는 글꼴을 Noto로 고른 경우에만 적용(SIL OFL 1.1). +1(`applyDelta` delta>0)일 때 숫자 확대(`jkBumpGrow`)와 `+1` 표식 상승(`jkBumpLift`). −1은 연출 없음. 사이버 스킨의 기존 입체 그림자는 그대로.

### APK
`app-debug-0910_0628-scorebump.apk` (`3193fcb`)

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1(Noto Sans KR 400/700/800, Jua, Do Hyeon, Orbitron). AndroidX Apache-2.0. Play 스토어 제출용이 아닌 debug APK.

### 보안
카메라·마이크 없음. 팀명 `textContent`. 사용자 HTML 삽입 없음. 표식 문구는 고정 `+`와 숫자만.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, help 3종, `index.html`, docs, Android assets www
- 신규 파일: `downloads/app-debug-0910_0628-scorebump.apk`
- 재사용: 기존 `.score-glyph` · `applyDelta` · `.point-num`
- 미수정: `MainActivity.kt`
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
`flashScoreBump` + `jkBumpGrow` + `jk-bump-tag` 묶음은 공개 GitHub에서 미발견(검색일 2026-09-10). `text-shadow` / `@keyframes` 확대는 일반 CSS.

### 특허 (검토 여지 · 비침해 보장 아님)
- 외부 리모컨의 눌림→뗌 경과시간·이동거리로 탭/스와이프 구분
- 좌표 없는 단독 시스템 터치-아웃사이드 신호를 특정 버튼 눌림으로 해석
- 득점 시 숫자 확대와 떠오르는 표식의 시각 타이밍 조합(일반 UI 관용에 가깝지만 별도 표시)

---
## 2026-09-10 원 간격·점수 칸 맞춤 (JK_DISC_GAP / JK_SCORE_FILL)

족구 보드 자체 HTML/CSS/JS만 변경. `MainActivity.kt` 미수정. `C:\Score` · `C:\PickleBoard` · `C:\sports_scoreboard` 미수정.

원 번호(왼쪽 3·4/2·1, 오른쪽 1·2/4·3)는 유지. 원 쌍을 점수 가운데로 모음. 큰 점수는 남은 칸(`cqw`/`cqh`)에 맞춤. 칸 쿼리를 못 쓰는 환경은 기존 clamp.

### APK
`app-debug-0910_0029-discscore.apk` (`5bcba58`)

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1. AndroidX Apache-2.0. Play 스토어 제출용이 아닌 debug APK.

### 보안
카메라·마이크 없음. 팀명 `textContent`. 사용자 HTML 삽입 없음.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, help 3종, `index.html`, docs, Android assets www
- 신규 파일: `downloads/app-debug-0910_0029-discscore.apk`
- 재사용: 기존 `.rot-dot` 슬롯 · `.point-num` · `paint`
- 미수정: `MainActivity.kt`
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
`JK_DISC_GAP` + `score-glyph` + `78cqw` 묶음은 공개 GitHub에서 미발견(검색일 2026-09-10). `container-type` / `cqh`는 일반 CSS.

### 특허 (검토 여지 · 비침해 보장 아님)
- 외부 리모컨의 눌림→뗌 경과시간·이동거리로 탭/스와이프 구분
- 좌표 없는 단독 시스템 터치-아웃사이드 신호를 특정 버튼 눌림으로 해석

---
## 2026-09-09 팀명 세트쪽·터치수정·새경기 선서브 (JK_NAME_MID / JK_NAME_EDIT)

족구 보드 자체 HTML/CSS/JS만 변경. `MainActivity.kt`·패드 제스처(`JK_PAD_GESTURE`) 미수정. `C:\Score` · `C:\PickleBoard` · `C:\sports_scoreboard` 미수정. 외부 구현 미복사.

팀명을 가운데 세트 쪽으로 옮기고 글자 크기를 2배로 함. 사이버 스킨은 위치를 위쪽 가운데로 두고 글자만 2배. 팀명 터치는 항상 이름 수정(최대 20자, `textContent`/`value`만). 선서브는 원·화면 좌우 점수·패드 위(오른쪽)·아래(왼쪽). 새 경기 카드는 취소 또는 팀명 선서브 버튼. 경기 승 창의 새 경기도 같은 카드.

### APK
`app-debug-0909_2357-nameserve.apk` (`d405b89`)

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1. AndroidX Apache-2.0. Play 스토어 제출용이 아닌 debug APK.

### 보안
카메라·마이크 없음. 팀명 `textContent`와 input `value`만. 사용자 HTML 삽입 없음.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, help 3종, `index.html`, docs, Android assets www
- 신규 파일: `downloads/app-debug-0909_2357-nameserve.apk`
- 재사용: 기존 `.banner` / `setServeChoice` / `jkHidPoint` / `applyDelta` / `JK_PAD_GESTURE`
- 미수정: `MainActivity.kt`
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
`paintFreshServeBtns` + `openNameBanner` + `startFreshWithServe` 묶음은 공개 GitHub에서 미발견(검색일 2026-09-09).

### 특허 (검토 여지 · 비침해 보장 아님)
- 외부 리모컨의 눌림→뗌 경과시간·이동거리로 탭/스와이프 구분
- 좌표 없는 단독 시스템 터치-아웃사이드 신호를 특정 버튼 눌림으로 해석

---
## 2026-09-09 패드 고정·세트 안내 중 득점 (JK_WIN_FLOW)

`MainActivity.kt`는 `app-debug-0909_2234-padgesture.apk`(`JK_PAD_GESTURE`)와 동일하게 유지. 점수판 HTML만 수정. `C:\Score` · `C:\PickleBoard` · `C:\sports_scoreboard` 미수정.

세트 안내 창(`is-match-over` 아님)이 열려 있어도 `jkHidPoint`/`applyDelta`가 득점을 삼키지 않고 안내만 닫는다. 경기 승 창과 새 경기 확인 창에서는 기존처럼 막는다. 위 버튼 인식 Kotlin은 손대지 않음.

### APK
`app-debug-0909_2331-padwin.apk` (`775a49a`)

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1. AndroidX Apache-2.0. Play 스토어 제출용이 아닌 debug APK.

### 보안
카메라·마이크 없음. 팀명 `textContent`. 사용자 HTML 삽입 없음.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, help 3종, `index.html`, docs, Android assets www
- 신규 파일: `downloads/app-debug-0909_2331-padwin.apk`
- 재사용: 기존 `jkHidPoint` / `applyDelta` / `closeWinBanner` / `JK_PAD_GESTURE`
- 미수정: `MainActivity.kt`
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
`clearSetWinNotice` + `isMatchWinBannerOpen` 묶음은 공개 GitHub에서 미발견(검색일 2026-09-09).

### 특허 (검토 여지 · 비침해 보장 아님)
- 외부 리모컨의 눌림→뗌 경과시간·이동거리로 탭/스와이프 구분
- 좌표 없는 단독 시스템 터치-아웃사이드 신호를 특정 버튼 눌림으로 해석

---
## 2026-09-09 세트 자동 닫힘·경기 승 새 경기 (JK_WIN_FLOW)

족구 보드 자체 HTML/CSS/JS만 변경. `MainActivity.kt`·패드 제스처(`JK_PAD_GESTURE`) 미수정. `C:\Score` · `C:\PickleBoard` · `C:\sports_scoreboard` 미수정. 외부 구현 미복사.

세트 종료 안내 창은 확인 버튼을 숨기고 5초 뒤 자동으로 닫혀 다음 세트를 칠 수 있게 한다. 경기 전체가 끝나면 같은 창에 확인(결과만 닫기)과 새 경기(바로 초기화, 두 번째 확인 없음)를 둔다. 기존 `runFreshMatch` / `closeWinBanner` 재사용.

### APK
`app-debug-0909_2256-winflow.apk`

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1: Noto Sans KR · Jua · Do Hyeon · Orbitron. AndroidX Apache-2.0. Play 스토어 제출용이 아닌 debug APK.

### 보안
카메라·마이크 없음. 팀명 `textContent`. 사용자 HTML 삽입 없음. `setTimeout`으로 로컬 타이머만.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, help 3종, `index.html`, `docs/jokgu-prd.md`, `docs/jokgu-plan.md`, `docs/jokgu-apk.md`, `docs/jokgu-changelog.md`, Android assets www
- 신규 파일: `downloads/app-debug-0909_2256-winflow.apk`
- 재사용: 기존 `openWinBanner` / `closeWinBanner` / `runFreshMatch` / `#winBanner`
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`. `MainActivity.kt` 미수정

### GitHub 코드 검색
`JK_WIN_FLOW` + `WIN_AUTO_MS` + `is-match-over` 묶음은 공개 GitHub에서 미발견(검색일 2026-09-09). `setTimeout`은 관용 JS.

### 특허 (검토 여지 · 비침해 보장 아님)
- 화면 좌우 점수 BLE GATT notify
- APK L/R 클릭·길게 −1
- 외부 리모컨의 눌림→뗌 경과시간·이동거리로 탭/스와이프 구분
- 좌표 없는 단독 시스템 터치-아웃사이드 신호를 특정 버튼 눌림으로 해석

---
## 2026-09-09 위·가운데·아래 패드 제스처 인식 — 실기기 확인 완료 (JK_PAD_GESTURE)

**최종본. 아래 두 항목(`JK_PAD_CAL3`, `JK_HID_PAD_LOCK`/`JK_HID_BAR_GUARD`의 위 버튼 처리)을 대체한다.**
`android/app/src/main/java/com/jokgu/scoreboard/MainActivity.kt` 변경. 갤럭시 탭 S9+(SM-X816N)에서 실제 리모컨으로 반복 테스트한 결과, 이 리모컨은 좌표를 찍는 방식이 아예 아니었다.

- **위 버튼**: 좌표가 있는 터치가 전혀 오지 않는다. OS가 상태 바 노출 제스처로 통째로 가져가며, 앱에는 좌표 없는 단독 `ACTION_OUTSIDE` 이벤트 하나만 온다(`FLAG_WATCH_OUTSIDE_TOUCH` 필요). 이 "눌림 없이 OUTSIDE 하나만 왔다"는 패턴 자체가 위 버튼 신호다. 이전 시도(상태 바 노출을 `WindowInsets` 리스너로 감지)는 타이밍이 안 맞아 작동하지 않았다.
- **가운데·아래**: 좌표 비교가 아니라 눌림→뗌 사이 경과시간·이동거리로 구분되는 제스처(탭=가운데/되돌리기, 아래로 스와이프=아래/왼쪽 득점)다. 그래서 3점을 아무리 정확히 찍어도(`JK_PAD_CAL3`) 좌표 판정 자체가 틀린 모델이라 안 맞았다.
- 보정은 가운데→아래 2단계만 남긴다(좌표는 "켬" 상태 확인용으로만 저장, 판정에는 쓰지 않음).

`C:\PickleBoard\android\app\src\main\java\com\pickleboard\app\MainActivity.kt`의 `HID_UD_BTN_GESTURE_v1`/`HID_UD_BTN_OUTSIDE_v1`가 겪은 것과 같은 문제였다. 동작 방식만 이해하고 코드는 Jokguboard 스타일로 새로 작성했다(변수명·함수 구조 다름, 볼륨 키 재주입 대신 기존 `jkHidPoint`/`jkHidUndo` 직접 호출).

### 실기기 확인
갤럭시 탭 S9+(SM-X816N)에 설치, 위·가운데·아래 각각 눌러 정상 동작(오른쪽 득점·되돌리기·왼쪽 득점) 확인 완료.

### APK
`app-debug-0909_2234-padgesture.apk`

### 라이선스
신규 npm 없음. GPL 없음. 새 의존성 없음.

### 보안
터치 좌표·타이밍만 다루는 로컬 로직. 네트워크 전송 없음.

### 신규 / 재사용 / 변경
- 변경: `MainActivity.kt`(`takeAlign`/`maybePadEvent`/`classifyPadGesture`/`emitPadZone`, dispatch* 전면 재작성), `jokgu_scoreboard.html`(+ Android assets 사본, `pad.*` i18n), `index.html`, `help.html`/`help_en.html`/`help_zh.html`(+ Android assets 사본), `docs/jokgu-prd.md`, `docs/jokgu-plan.md`, `docs/jokgu-apk.md`
- 재사용: 기존 `jkHidPoint`/`jkHidUndo`/`padPrefs`/`applyImmersive`
- 참고만 하고 코드는 복사하지 않음: `C:\PickleBoard`의 `HID_UD_BTN_GESTURE_v1`/`HID_UD_BTN_OUTSIDE_v1`(제스처 분류·단독 OUTSIDE 인식 개념)
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### 특허 (검토 여지 · 비침해 보장 아님)
- 외부 리모컨의 눌림→뗌 경과시간·이동거리로 탭/스와이프 구분해 서로 다른 버튼으로 매핑
- 좌표 없는 단독 시스템 터치-아웃사이드 신호를 특정 버튼 눌림으로 해석

### GitHub 코드 검색
`JK_PAD_GESTURE` + `ACTION_OUTSIDE` 기반 3버튼 리모컨 매핑 조합은 공개 GitHub에서 미발견(검색일 2026-09-09). `FLAG_WATCH_OUTSIDE_TOUCH`/`MotionEvent` 액션 분류는 Android 관용 API.

---
## 2026-09-09 위·가운데·아래 패드 3점 실측 보정 (JK_PAD_CAL3, 대체됨 — 위 항목 참고)

`android/app/src/main/java/com/jokgu/scoreboard/MainActivity.kt`, `jokgu_scoreboard.html`(+Android assets 사본) 변경. 실기기(갤럭시 탭 S9+, SM-X816N)에 설치해 테스트한 결과 "위/가운데/아래를 다 눌러봤는데 잘 안됨" 확인. 기존 `JK_HID_PAD`는 위·아래 두 점만 찍고 가운데(되돌리기) 판정은 두 점의 평균 좌표로 계산했는데, 실제 리모컨의 가운데 버튼 좌표가 기하학적 중간이 아니어서 판정이 어긋난 것으로 보인다. `C:\PickleBoard`가 겪었던 것과 같은 종류의 문제(`HID_UD_BTN_CAL_v1`, 위·가운데·아래 3점을 각각 실측)를 참고해 동작 방식만 이해하고 코드는 새로 작성했다.

- 보정 단계를 2단계(위→아래)에서 3단계(위→가운데→아래)로 변경. 각 버튼을 실제로 눌러 좌표를 각각 저장.
- 판정(`laneHit`)도 계산된 중간점이 아니라 저장된 세 점 중 가장 가까운 쪽으로 분류.
- 설정 화면 안내 문구·다국어(ko/en/zh)를 3단계에 맞게 갱신.

### APK
`android/app/build/outputs/apk/debug/app-debug.apk` 빌드 성공, 실기기(SM-X816N)에 설치해 UI·보정 흐름(위→가운데→아래 안내 전환)까지 확인. 저장소 `downloads/`에 정식 등록은 사용자가 실제 리모컨으로 최종 확인한 뒤 진행.

### 라이선스
신규 npm 없음. GPL 없음. 새 의존성 없음.

### 보안
좌표만 다루는 로컬 로직. 네트워크 전송 없음.

### 신규 / 재사용 / 변경
- 변경: `MainActivity.kt`(`takeAlign`/`laneHit`), `jokgu_scoreboard.html`(+ Android assets 사본, `pad.*` i18n·`jkPadNote`·`refreshPadBox`)
- 참고만 하고 코드는 복사하지 않음: `C:\PickleBoard`의 `HID_UD_BTN_CAL_v1`(3점 실측 개념)
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### 특허 (검토 여지 · 비침해 보장 아님)
- 포인터 Y 세 점을 각각 실측해 위/중/아래로 분류 (기존 "두 점" 항목을 세 점으로 갱신)

---
## 2026-09-09 위·가운데·아래 패드 신뢰성 개선 (JK_HID_PAD_LOCK / JK_HID_BAR_GUARD / JK_IMMERSIVE_REAPPLY)

**`JK_HID_BAR_GUARD`의 "상태 바 노출 감지 → 위 버튼 이김" 부분은 이후 `JK_PAD_GESTURE`(맨 위 항목)로 대체됨. `JK_HID_PAD_LOCK`·`JK_IMMERSIVE_REAPPLY`·`JK_HID_BAR_GUARD`의 "바 재숨김" 부분은 계속 유효.**
`android/app/src/main/java/com/jokgu/scoreboard/MainActivity.kt`만 변경. `C:\Score` · `C:\PickleBoard` · `C:\sports_scoreboard` 미수정·미복사. `C:\PickleBoard`가 겪었던 것과 같은 종류의 문제(같은 리모컨 눌림이 두 경로로 중복 도착, 몰입 모드 중 시스템 바가 살짝 열려 위 버튼 눌림을 앱이 못 받는 기기)를 참고해 동작 방식만 이해하고 코드는 Jokguboard 자체 스타일로 새로 작성했다. 기존 `JK_HID_PAD`(위·가운데·아래 좌표 2점 보정, 위=오른쪽 이김·가운데=되돌리기·아래=왼쪽 이김)는 아직 이 저장소에서 실기기로 검증 전이라, 문제를 겪기 전에 다음 3가지를 미리 반영했다.

- `JK_HID_PAD_LOCK`: 패드 터치·볼륨키·미디어키 중 같은 눌림이 두 경로로 들어와도 짧은 잠금(400ms)으로 한 번만 반영. 득점·되돌리기 각각 별도 타이머.
- `JK_HID_BAR_GUARD`: 몰입 모드 중 상태·내비 바가 살짝 보이면 280ms 뒤 다시 숨김. 맞추기가 끝났고 가운데·아래 입력이 최근(450ms) 없었는데 바가 새로 보이면, OS가 위 버튼 터치를 통째로 가로챈 것으로 보고 위 버튼 이김(오른쪽 +1)으로 대신 처리.
- `JK_IMMERSIVE_REAPPLY`: 설정 화면 등에서 포커스를 되찾을 때도 몰입 모드 재적용.

### APK
빌드 없음(코드만 변경, 이 세션에서 새 APK 산출 없음). 실기기 확인 후 `app-debug-MMDD_HHMM-` 이름으로 새로 빌드해 `downloads/`에 추가하고 이 파일·`jokgu-apk.md`·`jokgu-plan.md`·`jokgu-prd.md`·`index.html`/`help*.html`의 현재 파일명을 함께 갱신할 것.

### 라이선스
신규 npm 없음. GPL 없음. 새 AndroidX 의존성 없음(기존 core-ktx/appcompat/webkit만 사용).

### 보안
좌표·타이밍만 다루는 로컬 로직. 네트워크 전송 없음. 카메라·마이크 없음.

### 신규 / 재사용 / 변경
- 변경: `android/app/src/main/java/com/jokgu/scoreboard/MainActivity.kt`, `docs/jokgu-prd.md`, `docs/jokgu-plan.md`
- 재사용: 기존 `padPrefs`/`laneHit`/`takeAlign`/`boardJs`/`applyImmersive`
- 참고만 하고 코드는 복사하지 않음: `C:\PickleBoard\android\app\src\main\java\com\pickleboard\app\MainActivity.kt`의 `HID_UD_BTN_*` 계열 마커(디바운스, WindowInsets 감지, 몰입 재적용 개념)
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### 특허 (검토 여지 · 비침해 보장 아님)
- 포인터 Y 두 점을 저장한 뒤 위/중/아래로 분류 (기존 항목, 계속 적용)
- 시스템 바 노출을 감지해 보정된 버튼 눌림으로 대체 처리
- 서로 다른 입력 경로(터치 좌표·볼륨키·미디어키)의 중복 눌림을 짧은 잠금 창으로 병합

### 확인 (2026-09-09 갤럭시 탭 S9+ 실기기)
로컬 Gradle 빌드가 처음엔 Cursor Kotlin 언어 서버가 빌드 산출물(`R.jar`)을 점유해 `processDebugResources`에서 막혔다(코드 문제 아님, 파일 잠금 — 언어 서버 프로세스 종료 후 정상 빌드). 빌드 성공 후 실기기에 설치·실행해 크래시 없음, `JK_HID_BAR_GUARD`의 상태 바 재숨김을 스와이프로 직접 재현해 확인. 다만 위·가운데·아래 실사용 테스트에서 보정이 어긋나는 문제가 나와 `JK_PAD_CAL3`(위 항목)로 이어졌다.

---
## 2026-09-09 코트 원 2×2 (JK_COURT_DISC / JK_SERVE_ONE)

족구 보드 자체 CSS·HTML만 변경. `C:\Score` · `C:\PickleBoard` · `C:\sports_scoreboard` 미수정. 외부 구현 미복사.

선수 원을 점수 영역 바깥 세로 열에서 점수 숫자 위·아래로 옮김. 화면 왼쪽 팀 위 3·4 / 아래 2·1, 화면 오른쪽 팀 위 1·2 / 아래 4·3. 엔드 스왑 시에도 화면 왼쪽/오른쪽 기준을 유지. 원 숫자는 슬롯에 고정. 족구 선서브 금색 원은 1번부터. 사이드아웃 1→2→3→4와 득점 규칙은 그대로.

### APK
`app-debug-0909_1701-courtdisc.apk`

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1: Noto Sans KR · Jua · Do Hyeon · Orbitron. AndroidX Apache-2.0 (`core-ktx` 1.12.0, `appcompat` 1.6.1, `webkit` 1.10.0). Play 스토어 제출용이 아닌 debug APK.

### 보안
카메라·마이크 없음. 팀명 `textContent`. 사용자 HTML 삽입 없음. 점수 탭·원 탭 분리 유지.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, help 3종, `index.html`, `docs/jokgu-prd.md`, `docs/jokgu-plan.md`, `docs/jokgu-apk.md`, `docs/jokgu-changelog.md`, Android assets www
- 신규 파일: `downloads/app-debug-0909_1701-courtdisc.apk`
- 재사용: 기존 `.rot-dot` / `serveIdxBlue` / `serveIdxWhite` / `setServeChoice` / `.side-main`
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
`JK_COURT_DISC` + `JK_SERVE_ONE` + 화면 왼쪽 3·4/2·1 묶음은 공개 GitHub에서 미발견(검색일 2026-09-09). `display: contents` / CSS grid는 관용 CSS.

### 특허 (검토 여지 · 비침해 보장 아님)
- 화면 좌우 점수 BLE GATT notify
- APK L/R 클릭·길게 −1
- 3D 스킨(원근·카드 기울기) 구성
- HID 미디어/음량 키를 점수·되돌리기에 연결
- 포인터 Y 두 점을 저장한 뒤 위/중/아래로 분류

---
## 2026-09-08 HID 패드 (JK_HID_PAD / JK_PAD_ALIGN)

족구 보드 자체 코드만 확장. `C:\Score` · `C:\PickleBoard` · `C:\sports_scoreboard` 미수정. 외부 구현 미복사.

APK에서만 설정 맨 아래에 「위·가운데·아래 버튼 리모컨」 칸을 둔다. 웹에는 칸 없음. 「위·아래 위치 맞추기」는 위 짧게 한 번, 아래 짧게 한 번. 간격이 작으면 아래만 다시. 「다시 맞추기」는 이 기기 SharedPreferences(`jokgu_pad_marks`)만 지움. 맞춘 뒤 그 세로줄: 위=화면 오른쪽 +1, 가운데=되돌리기, 아래=화면 왼쪽 +1. 애매하면 삼키고 팀명 클릭이 나가지 않음. 맞추기 전 위·중·아래를 점수로 쓰지 않음. USB 마우스 좌우는 세로줄 밖에서 기존 `JK_APK_LR`. 손가락 터치는 기존 그대로.

원형 재생/일시정지(85)·이전(88)·다음(87) 짧게=되돌리기. 스캔 320은 점수·되돌리기 없음. 외부 BT 음량만: 짧게 25=왼쪽 +1, 짧게 24=오른쪽 +1, 길게=되돌리기. 본체 음량은 OS. HID +1 후 1.5초 동안 다음 HID 득점과 음량 길게 되돌리기 차단. 가운데 되돌리기는 차단하지 않음. 득점 규칙은 기존 `applyDelta`.

### APK
`app-debug-0908_2010-hidpad.apk`

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1: Noto Sans KR · Jua · Do Hyeon · Orbitron. AndroidX Apache-2.0 (`core-ktx` 1.12.0, `appcompat` 1.6.1, `webkit` 1.10.0). Play 스토어 제출용이 아닌 debug APK.

### 보안
카메라·마이크 없음. `AndroidPad`는 이 기기 SharedPreferences 실수 좌표만. `evaluateJavascript`는 고정 리터럴(`L`/`R`/undo 플래그). 팀명 `textContent`. 사용자 HTML 삽입 없음.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, `MainActivity.kt`, help 3종, `index.html`, `docs/jokgu-prd.md`, `docs/jokgu-plan.md`, `docs/jokgu-apk.md`, `docs/jokgu-changelog.md`, Android assets www
- 신규 파일: `downloads/app-debug-0908_2010-hidpad.apk`
- 재사용: 기존 `applyDelta` / `undoLast` / `JK_APK_LR` / `AndroidShell` / 설정 시트
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
`JK_HID_PAD` + `jkHidPoint` + `jokgu_pad_marks` 묶음은 공개 GitHub에서 미발견(검색일 2026-09-08). `dispatchKeyEvent` / `SharedPreferences` / `evaluateJavascript`는 Android 관용 API.

### 특허 (검토 여지 · 비침해 보장 아님)
- 화면 좌우 점수 BLE GATT notify
- APK L/R 클릭·길게 −1
- 3D 스킨(원근·카드 기울기) 구성
- HID 미디어/음량 키를 점수·되돌리기에 연결
- 포인터 Y 두 점을 저장한 뒤 위/중/아래로 분류

---
## 2026-09-02 원 바깥·크게 (JK_NET_COL / JK_DISK_GLYPH)

선수 원을 가운데가 아니라 바깥(왼쪽 팀 왼쪽, 오른쪽 팀 오른쪽)에 세로로 둠. 왼쪽 아래=1, 오른쪽 위=1 유지. 원·숫자를 키우고 서브 선수 원·숫자만 더 크게, 금색 배경. 숫자 자리 이동 없음. 서브 규칙은 그대로. `C:\Score` 미수정.

### APK
`app-debug-0902_1746-outdisc.apk`

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1: Noto Sans KR · Jua · Do Hyeon · Orbitron. AndroidX Apache-2.0.

### 보안
기존과 동일. 카메라·마이크 없음. CSS만 변경.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, help 3종, `index.html`, `docs/jokgu-prd.md`, Android assets www
- 신규 파일 없음 (APK `downloads/app-debug-0902_1746-outdisc.apk`)
- 재사용: 기존 `.rot-dot` / `.rot-dot.is-server`
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
`JK_NET_COL` + `rot-dot.is-server` 크기 clamp는 공개 묶음 미발견(검색일 2026-09-02). `clamp()` 원 크기는 관용 CSS.

### 특허 (검토 여지 · 비침해 보장 아님)
- 화면 좌우 점수 BLE GATT notify
- APK L/R 클릭·길게 −1
- 3D 스킨(원근·카드 기울기) 구성

---
## 2026-09-02 네트쪽 세로 원·서브 번호 고정 (JK_NET_COL / JK_ROTATE)

선수 원을 네트 쪽에 세로로 둠. 왼쪽 팀은 아래가 1번, 오른쪽 팀은 위가 1번. 원 숫자는 자리를 옮기지 않고 서브만 금색 배경. 족구는 랠리 득점, 사이드아웃 시 직전 서브 팀만 1→2→3→4. Futnet 강제 회전 없음. `C:\Score` 미수정.

### APK
`app-debug-0902_1738-netcol.apk`

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1: Noto Sans KR · Jua · Do Hyeon · Orbitron. AndroidX Apache-2.0.

### 보안
기존과 동일. 카메라·마이크 없음. 팀명 `textContent`. 사용자 입력 HTML 삽입 없음.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, help 3종, `index.html`, `docs/jokgu-prd.md`, `docs/jokgu-plan.md`, `scripts/check-win-rules.js`, Android assets www
- 신규 파일 없음 (APK `downloads/app-debug-0902_1738-netcol.apk`)
- 재사용: 기존 `.rot-dot` / `serveIdxBlue` / `serveIdxWhite` / `#btnFresh`
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
`JK_NET_COL` + `bumpServeIdx` 묶음은 공개 GitHub에서 미발견(검색일 2026-09-02). `flex-direction: column-reverse` 는 관용 CSS.

### 특허 (검토 여지 · 비침해 보장 아님)
- 화면 좌우 점수 BLE GATT notify
- APK L/R 클릭·길게 −1
- 3D 스킨(원근·카드 기울기) 구성

---
## 2026-09-02 사이버·링 새 경기 클릭 (JK_FRESH_HIT)

사이버·링 모습에서 가운데 칸이 3D로 뒤로 밀려 새 경기가 좌우 점수에 가려지던 문제를 수정. 가운데 `.mid` / `.sets-panel` 과 버튼만 `transform`을 해제. 좌우 카드 3D는 유지. `C:\Score` 미수정.

### APK
`app-debug-0902_1712-cyberhit.apk`

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1: Noto Sans KR · Jua · Do Hyeon · Orbitron. AndroidX Apache-2.0.

### 보안
기존과 동일. 카메라·마이크 없음. CSS만 변경, 사용자 입력 HTML 삽입 없음.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, help 3종, `index.html`, Android assets www
- 신규 파일 없음 (APK `downloads/app-debug-0902_1712-cyberhit.apk`)
- 재사용: 기존 `#btnFresh` / `#freshBanner` / `JK_SCORE_SKIN_BRIDGE`
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
`JK_FRESH_HIT` + `look-cyber3d` + `btnFresh` 묶음은 공개 GitHub에서 미발견(검색일 2026-09-02). `transform: none` 은 관용 CSS.

### 특허 (검토 여지 · 비침해 보장 아님)
- 화면 좌우 점수 BLE GATT notify
- APK L/R 클릭·길게 −1
- 3D 스킨(원근·카드 기울기) 구성

---
## 2026-09-02 가운데 칸을 점수 위에 (JK_FRESH_HIT)

스킨에서 좌우 큰 점수가 가운데 **새 경기**를 덮어 클릭이 점수/선서브로 가던 문제를 수정. `.mid` z-index 12. APK L/R에서 `.mid` 전체 제외. `C:\Score` 미수정.

### APK
`app-debug-0902_1646-midz.apk`

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1: Noto Sans KR · Jua · Do Hyeon · Orbitron. AndroidX Apache-2.0.

### 보안
기존과 동일. 카메라·마이크 없음.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, help 3종, `index.html`, Android assets www
- 신규 파일 없음 (APK `downloads/app-debug-0902_1646-midz.apk`)
- 재사용: `#btnFresh` / `#freshBanner`
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
가운데 스택 z-index는 관용 CSS. `JK_FRESH_HIT` 묶음 공개 구현 미발견(검색일 2026-09-02).

### 특허 (검토 여지 · 비침해 보장 아님)
- 화면 좌우 점수 BLE GATT notify
- APK L/R 클릭·길게 −1

---
## 2026-09-02 새 경기 클릭·금색 큰 버튼 (JK_FRESH_HIT)

0:0 선서브 대기에서 화면 L/R이 `#btnFresh` 클릭을 가로채던 것을 제외. 가운데 버튼을 키우고 금색 배경. `C:\Score` 미수정.

### APK
`app-debug-0902_1636-freshhit.apk`

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1: Noto Sans KR · Jua · Do Hyeon · Orbitron. AndroidX Apache-2.0.

### 보안
기존과 동일. 카메라·마이크 없음.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, help 3종, `index.html`, `docs/jokgu-plan.md`, Android assets www
- 신규 파일 없음 (APK `downloads/app-debug-0902_1636-freshhit.apk`)
- 재사용: 기존 `freshMatch` / `#freshBanner`
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
`JK_FRESH_HIT` — 공개 GitHub에서 동일 식별자 미발견(검색일 2026-09-02). closest 예외 목록은 관용 DOM 코드.

### 특허 (검토 여지 · 비침해 보장 아님)
- 화면 좌우 점수 BLE GATT notify
- APK L/R 클릭·길게 −1

---
## 2026-09-02 새 경기 확인 배너 (JK_FRESH_BANNER)

가운데 **새 경기**를 누르면 브라우저 `confirm` 대신 기존 `.banner` 카드에서 취소·확인을 고릅니다. 확인 시에만 점수·세트·서브·회전을 지웁니다. 팀명·종목·모습은 유지. `C:\Score` 미수정. Score HTML/CSS/JS는 복사하지 않음.

### APK
`app-debug-0902_1631-fresh.apk`

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1: Noto Sans KR · Jua · Do Hyeon · Orbitron.
AndroidX Core/AppCompat/WebKit (Apache-2.0).

### 보안
문구는 `textContent`/기존 I18N. 사용자 입력 HTML 삽입 없음. 배너가 열린 동안 +1/−1 차단. 카메라·마이크 없음.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, `help.html`, `help_en.html`, `help_zh.html`, `index.html`, `docs/jokgu-plan.md`, Android assets www
- 신규 파일 없음 (APK `downloads/app-debug-0902_1631-fresh.apk`)
- 재사용: 기존 `.banner` / `confirmFresh` / `freshMatch` 초기화 로직
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
`JK_FRESH_BANNER`, `runFreshMatch`, `isFreshBannerOpen` — 공개 GitHub에서 동일 식별자 묶음 미발견(검색일 2026-09-02). 취소/확인 오버레이는 관용 UI.

### 특허 (검토 여지 · 비침해 보장 아님)
청구항 대조는 법률 자문이 아니다.
- 화면 좌우 점수 BLE GATT notify
- APK L/R 클릭·길게 −1
- 세트 후 자동 엔드 스왑

일반 확인 대화상자는 관용적 UI이다.

---
## 2026-09-02 족구 4인·Futnet 종목 전환 (JK_SPORT_RULES)

한 HTML에서 설정으로 `jokgu` / `futnet`만 바꿉니다. 족구는 코트 4명·15점·3세트 2선승, 사이드아웃 시 1→2→3→4. Futnet은 11점·최대 15·항상 3세트 2선승, 득점자 서브, 더블/트리플은 원 탭으로 서브 표시, 싱글은 ①+짝수 R/홀수 L. 종목 변경 시 확인 후 점수만 초기화. `C:\Score` · `C:\PickleBoard` · `C:\sports_scoreboard` 미수정. 신규 npm/GPL 없음.

### APK
`app-debug-0902_1618-sport.apk`

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL 1.1: Noto Sans KR · Jua · Do Hyeon · Orbitron (Google Fonts CSS만 로드).
AndroidX Core/AppCompat/WebKit, Android Gradle Plugin, Kotlin — Apache-2.0.

### 보안
종목·유형 id는 `jokgu|futnet`, `single|double|triple`만 허용. 팀명 `textContent`. `confirm` 후 기록 초기화. 카메라·마이크 없음. innerHTML로 사용자 입력 삽입 없음.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, `scripts/check-win-rules.js`, `help.html`, `help_en.html`, `help_zh.html`, `index.html`, `docs/jokgu-plan.md`, `docs/jokgu-prd.md`, Android assets www
- 신규 파일 없음 (APK 산출물 `downloads/app-debug-0902_1618-sport.apk`)
- 재사용: 기존 `applyDelta` / `snapshot` / `restore` / `paint`
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### GitHub 코드 검색
`JK_SPORT_RULES`, `isFutnetSetWon` — 공개 GitHub에서 동일 식별자·동일 구현 미발견(검색일 2026-09-02). 일반 승패 비교식은 관용 코드.

### 특허 (검토 여지 · 비침해 보장 아님)
청구항 대조는 법률 자문이 아니다.
- 화면 좌우 점수 BLE GATT notify
- APK L/R 클릭·길게 −1
- 세트 후 자동 엔드 스왑·다음 서브
- 한 화면에서 종목 전환 시 점수 초기화 + 선수 원 개수 변경
- 사이드아웃 회전 vs 득점자 서브 전환

일반 점수 표시·15점/11점 듀스 규칙은 종목 규칙의 관용적 구현이다.

### Play Store 참고
디버그 APK는 스토어 제출용이 아님. 신규 GPL 없음. 카메라·마이크 권한 없음. 스토어 등록 시에는 release 서명·개인정보 문구가 별도로 필요하다.

---
## 2026-09-02 가운데 새 경기·서브 원 금색 (JK_MID_PANEL)

가운데 세트 칸 폭 확대. 새 경기를 세트 칸으로 이동. 심플에서는 새 경기 숨김, 하단은 되돌리기·전체표시. 서브 원 금색 배경. `C:\Score` 미수정.

### APK
`app-debug-0902_1600-mid.apk`

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL: Noto Sans KR · Jua · Do Hyeon · Orbitron.
AndroidX Core/AppCompat/WebKit (Apache-2.0).

### 보안
팀명 `textContent`. APK에서 `#btnFresh` 클릭은 점수 L/R에서 제외. 카메라·마이크 없음.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, `help.html`, `help_en.html`, `help_zh.html`, `index.html`, `docs/jokgu-plan.md`, Android assets www
- 신규 파일 없음
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### 특허 (검토 여지 · 비침해 보장 아님)
- 화면 좌우 점수 BLE GATT notify
- APK L/R 클릭·길게 −1
- 세트 후 자동 엔드 스왑·다음 서브

---
## 2026-09-02 하단 메뉴·설정 언어·큰 점수 (JK_DOCK)

메뉴(되돌리기·심플·새 경기·설정·도움말)를 보드 아래 한 줄 오른쪽으로. 한국어·English·中文은 설정 저장. 양팀 −1 버튼 삭제(길게 −1 유지). 일반 점수 2배, 심플 1.5배. `C:\Score` 미수정.

### APK
`app-debug-0902_1550-dock.apk`

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL: Noto Sans KR · Jua · Do Hyeon · Orbitron.
AndroidX Core/AppCompat/WebKit (Apache-2.0).

### 보안
팀명 `textContent`. 언어는 설정 select의 ko/en/zh만 허용. 카메라·마이크 없음.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html`, `help.html`, `help_en.html`, `help_zh.html`, `index.html`, `docs/jokgu-plan.md`, Android assets www
- 신규 파일 없음
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### 특허 (검토 여지 · 비침해 보장 아님)
- 화면 좌우 점수 BLE GATT notify
- APK L/R 클릭·길게 −1
- 세트 후 자동 엔드 스왑·다음 서브

---
## 2026-09-02 Score 스킨 CSS 연결 (JK_SCORE_SKIN_CSS)

자체 `C:\Score\voice_scoreboard.html`의 스킨 CSS만 추출해 족구 DOM에 연결. Score 폴더는 수정하지 않음. HTML·JS·카메라·마이크·Pro 자막·단복식·핸디·11점은 이식하지 않음. PNG `url("img/…")`와 `skin-liverec`은 제외. `#court`에 `pointer-events: none`을 두지 않음.

`glow`→`skin-neon`, `paper`→`skin-card`, 나머지 look id는 같은 이름 `skin-*`. `plain`·`field`는 기존 look 팔레트만. 서브 표시에 `is-serving`을 함께 둠.

### APK
`app-debug-0902_1519-skins.apk`

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL: Noto Sans KR · Jua · Do Hyeon · Orbitron.
AndroidX Core/AppCompat/WebKit (Apache-2.0). Kotlin/AGP (Apache-2.0).
스킨 CSS는 이 제품군의 자체 Score 파일에서 재사용. 제3자 GitHub 구현 아님.

### 보안
CSS·class만 추가. 팀명은 `textContent`. 카메라·마이크 권한 없음. 외부 URL PNG 요청 없음.

### 신규 / 재사용 / 변경
- 변경: `jokgu_scoreboard.html` (JK_SCORE_SKIN_CSS · JK_SCORE_SKIN_BRIDGE · applyLook), `help.html`, `help_en.html`, `help_zh.html`, `index.html`, `docs/jokgu-plan.md`, Android assets www
- 재사용: 자체 Score 스킨 CSS 구간 (NEON~ARENA3D, 변환 후)
- 신규 파일 없음 (추출 스크립트는 TEMP만, 저장소 미포함)
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### 특허 (검토 여지 · 비침해 보장 아님)
- 화면 좌우 점수 BLE GATT notify
- APK L/R 클릭·길게 −1
- 세트 후 자동 엔드 스왑·다음 서브
- 점수 TTS 문구 타이밍
- 3D 카드 기울기·스킨 프레임 표시

---
## 2026-09-02 Score형 보드 클래스·1~4 원·사이드아웃 로테이션 (JK_ROTATE)

`voice_scoreboard.html` 미복사. 단복식·핸디·11점·카메라·마이크·Pro 자막 없음.
팀 패널에 Score형 class(`team blue/white`, `sets-panel`, `score`)와 −1 버튼.
원 1~4 세로, 4번은 대기. 리시브 득점 시 1~3만 회전·서브권 이동.
심플·OFL·TTS·BLE·스킨 목록 유지. `C:\Score` 미수정.

### APK
`app-debug-0902_1508-rotate.apk`

---
## 2026-09-02 카드 테두리·3D 원근 (JK_FRAME_SVG / JK_BOARD_DEPTH)

청/백/가운데 레이아웃 유지. `#court`를 `.board-wrap`으로 감싸 원근만 추가.
`cyber3d`·`arena3d`에서만 좌우 카드 15° tilt. `arena`·`broadcast`는 SVG 테두리.
liverec·탁구장 PNG 없음. Score HTML/`#boardsWrap` 미복사. `C:\Score` 미수정.

### APK
`app-debug-0902_1449-card-depth.apk`

---
## 2026-09-02 TTS·BLE·스킨·심플·서체·세트제 (JK_SKIN_REMAP / JK_SIMPLE / JK_TTS / JK_BLE)

자체 코드만 확장. `C:\Score` · `C:\PickleBoard` · `C:\sports_scoreboard` 미수정. `voice_scoreboard.html` 미복사.

### 한 일
- 보드 모습을 족구 DOM(`.side.cyan` / `.side.ivory` / `.mid`)용 팔레트로 추가. liverec·탁구장 사진 없음. live/live2는 나중에 사진 교체 가능한 단색 배경.
- 심플 모드(큰 점수·세트, 크롬 숨김). 선서브 후 진입.
- OFL 글꼴: Jua, Noto Sans KR, Do Hyeon, Orbitron.
- 세트제: 2/3/4선승(기본 `bo3_2`).
- TTS: 득점·서브·세트·경기. Android `AndroidTTS`, 웹은 `speechSynthesis`. MP3 팩·마이크 없음.
- BLE: Score 시계와 같은 GATT UUID·패킷 v2 `[2, L게임, R게임, L세트, R세트]`. JS `AndroidBleScore.setBoardScore` / `setBoardScoreForce`.

### APK
`app-debug-0902_1430-tts-ble.apk`

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL: Noto Sans KR · Jua · Do Hyeon · Orbitron.
AndroidX Core/AppCompat/WebKit (Apache-2.0). Kotlin/AGP (Apache-2.0).
재사용: `BleScoreSpikeServer.kt`를 이 저장소 패키지(`com.jokgu.scoreboard`)로 이식. UUID는 시계 호환을 위해 동일.

### 보안
팀명 `textContent`. 점수는 기기 localStorage만. 마이크·카메라 없음.
BLE는 페어링된 기기만 read/notify. Android 12+ `BLUETOOTH_CONNECT`·`BLUETOOTH_ADVERTISE` 런타임 요청.
Play 스토어: BLE advertise 권한 고지 필요. `RECORD_AUDIO` 없음.

### 신규 / 재사용
- 변경: `jokgu_scoreboard.html`, `index.html`, `help.html`, `help_en.html`, `help_zh.html`, `LICENSE-FONTS.txt`, `docs/jokgu-plan.md`, `android/app` Manifest·MainActivity, Android assets www
- 신규: `android/app/src/main/java/com/jokgu/scoreboard/BleScoreSpikeServer.kt` (자체 Score 서버 이식)
- 재사용: Gradle wrapper, AndroidX
- 외부 미수정: `C:\Score`, `C:\PickleBoard`, `C:\sports_scoreboard`

### 특허 (검토 여지 · 비침해 보장 아님)
- 화면 좌우 점수 BLE GATT notify
- APK L/R 클릭·길게 −1
- 세트 후 자동 엔드 스왑·다음 서브
- 점수 TTS 문구 타이밍

---
## 2026-09-02 보드 모습 4종 (JK_LOOK_PACK)

설정에서 기본·네온·카드·경기장. Score 스킨 CSS 미복사. 카메라 없음. 규칙 무변.

### APK
`app-debug-0902_1408-look-pack.apk`

---
## 2026-09-02 보드 모습 (JK_BOARD_LOOK)

탁구 `voice_scoreboard.html` 미복사. 족구 HTML만 청 어두운 카드·백 밝은 카드·바깥 서브 띠로 다시 그림. 규칙·localStorage 키 무변. Score 미수정.

### APK
`app-debug-0902_1357-board-look.apk`

---

## 2026-09-02 족구 MVP (1단계)

그린필드 신규. `C:\Score` · `C:\PickleBoard` · `C:\sports_scoreboard` 미수정.

### 한 일
- `jokgu_scoreboard.html` 단일 점수판 (15점·14:14 듀스·3세트 2선승)
- 선서브 `serveChoice` → 첫 득점 `serveAnchor`, 세트 종료 코트 교대
- 되돌리기 · 새 경기 · 설정(팀명) · ko/en/zh · 도움말 3종
- 선수 원 1·2·3 표시(로테이션 로직은 다음 단계)
- Android WebView `com.jokgu.scoreboard`, 경기 중 L/R 클리커
- Vercel `/play`, `serve.ps1`, `jokgu_scoreboard_state_v1`

### APK
`app-debug-0902_1334-jokgu-mvp.apk`

### 라이선스
신규 npm 없음. GPL 없음. SIL OFL: Noto Sans KR · Jua. AndroidX AppCompat/WebKit(Apache-2.0).

### 보안
팀명 `textContent`. 점수는 기기 localStorage만. 마이크·카메라·BLE 권한 없음.

### 신규 / 재사용
- 신규: 이 저장소의 웹·Android·docs 전부
- 재사용 파일 없음 (Gradle wrapper JAR는 Gradle 배포본)

### 특허
침해 없음을 보장하지 않음. 클리커 L/R·자동 엔드 스왑은 검토 여지 있음.
